CREATE TABLE CUSTOMER_TBL (
    CUSTOMER_ID VARCHAR2(20) PRIMARY KEY,
    FULL_NAME VARCHAR2(50),
    EMAIL VARCHAR2(50),
    MOBILE VARCHAR2(15),
    PREFERRED_PACKAGE VARCHAR2(20)
);

INSERT INTO CUSTOMER_TBL
VALUES ('CUST1001','Ankit Sharma','ankit.sharma@example.com','9876501111','PORTRAIT');

INSERT INTO CUSTOMER_TBL
VALUES ('CUST1002','Riya Nair','riya.nair@example.com','9876502222','WEDDING');

INSERT INTO CUSTOMER_TBL
VALUES ('CUST1003','Mohit Verma','mohit.verma@example.com','9876503333','BASIC');

COMMIT;

CREATE TABLE BOOKING_TBL (
    BOOKING_ID NUMBER PRIMARY KEY,
    CUSTOMER_ID VARCHAR2(20),
    SHOOT_DATE DATE,
    START_TIME VARCHAR2(10),
    END_TIME VARCHAR2(10),
    PACKAGE_TYPE VARCHAR2(20),
    STATUS VARCHAR2(20),
    CONSTRAINT fk_customer
        FOREIGN KEY (CUSTOMER_ID)
        REFERENCES CUSTOMER_TBL(CUSTOMER_ID)
);


SELECT * FROM CUSTOMER_TBL;

CREATE SEQUENCE booking_seq
START WITH 510001
INCREMENT BY 1;

SELECT * FROM BOOKING_TBL;

SELECT booking_seq.NEXTVAL FROM dual;
